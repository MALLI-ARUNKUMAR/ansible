- name: variable perference
  hosts: local
  connection: local
  #vars:
   #NAME: "Ansible from play"
   #vars_files:
  #- course.yaml
  #vars_prompt:
  #- name: NAME
    #prompt: please enter your name
    #private: false
  tasks:
  - name: print name
    # vars:
    #  NAME: "Ansible from task"
    ansible.builtin.debug:
      msg: "hi {{ NAME }}"        


    # 1 args
    # 2 task  
    # 3 file
    # 4 prompt
    # 5 play
    # 6 inventory