- name: print username and password
  hosts: local
  connections: local
  vars_prompt:
    - name: username
      prompt: enter username
      private: false
    - name: password
      prompt: enter password
      private: true
  tasks:
    - name: print info
      ansible.builtin.debug:
        meg: " username is {{ username }}, password is {{ password }}"      